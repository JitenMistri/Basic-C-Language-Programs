#include<stdio.h>

int main(){
    int n,sum=0;
    printf("enter the number: ");
    scanf("%d", &n);
    while(n>0){
        int r = n%10;
        sum+=r;
        n = n/10;
    }
    printf("the sum of the number is %d", sum);

    return 0;
}
